// Shares the single Surah list with the web client, exactly as the locale files are shared,
// so a name or an ayah count cannot drift between the two teacher-facing clients.
export { SURAHS, SURAH_NUMBERS, surahName, ayahCount, rangeLabel } from "../../frontend/src/lib/surahs";
